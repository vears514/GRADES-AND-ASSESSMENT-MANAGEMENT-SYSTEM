# Grades & Assessment Management System

## Getting Started

### Prerequisites
- Node.js 18+
- npm/yarn/pnpm
- Firebase project
- Vercel account (for deployment)

### Installation

```bash
# Install dependencies
npm install

# Create environment file
cp .env.example .env.local

# Run development server
npm run dev
```

Visit `http://localhost:3000` to see the application.

### Environment Variables

Create `.env.local` with your Firebase credentials:

```
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_API_URL=http://localhost:3000/api
```

### Project Structure

```
src/
├── app/              # Next.js app directory
├── components/       # React components
├── lib/              # Utilities and helpers
├── services/         # Business logic
├── types/            # TypeScript types
├── hooks/            # Custom React hooks
└── styles/           # Global styles

public/              # Static files
firebase/            # Firebase configuration
```

### Available Commands

- `npm run dev` - Start development server
- `npm run build` - Build for production
- `npm start` - Start production server
- `npm run lint` - Run ESLint
- `npm run format` - Format code with Prettier
- `npm run type-check` - Check TypeScript types

## Features

- Grade encoding with draft functionality
- Multi-level grade verification
- Student grade viewing portal
- Grade correction workflows
- Advanced reporting and analytics
- Role-based access control
- Complete audit trails

## Tech Stack

- **Frontend**: Next.js 14, React 18, TypeScript, Tailwind CSS
- **Backend**: Next.js API Routes, Firebase
- **Database**: Firestore, Firebase Storage
- **Authentication**: Firebase Auth
- **Deployment**: Vercel

## Documentation

- See `/PROJECT_PLAN.md` for project overview
- See `/ARCHITECTURE.md` for system design
- See `/MODULE_SPECIFICATIONS.md` for detailed specs
- See `/UI_UX_WIREFRAMES.md` for design guidelines
